package com.plugin.core.proxy;

/**
 * 由于service的特殊性，采用欺骗的方式加载插件Service时只能同时存在一个实例
 * <p/>
 * 所以这里仍然通过service代理的方式来支持多Service
 *
 * @author cailiming
 */
public class PluginProxyService1 extends PluginProxyService {
}
